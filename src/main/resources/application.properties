spring.application.name=MUSIC-BACKEND
server.port=5555

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/musicdb
spring.datasource.username=root
spring.datasource.password=Somu@512006
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Exclude JPA (since you’re not using it)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# Optional logging for debugging SQL
logging.level.org.springframework.jdbc.core=DEBUG
